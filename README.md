
##  To start:

Inside `react` folder:

  
    1.Run `npm install`
  
    2.Run `npm start`
  
    3.See the result on your browser
    
    
Inside `websocket` folder:
    
    1.Java -jar pushserver.jar 8080
